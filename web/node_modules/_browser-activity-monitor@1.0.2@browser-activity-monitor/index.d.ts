/// <reference types="eventemitter2" />
import { EventEmitter2 } from 'eventemitter2';
export interface Options {
    /**
     * After how much inactivity do we consider a user inactive? (in ms)
     */
    inactivityThreshold: number;
}
export declare const ACTIVE = "ACTIVE";
export declare const INACTIVE = "INACTIVE";
export declare class ActivityMonitor extends EventEmitter2 {
    private document;
    private options;
    private _onVisibilityChange;
    private state;
    constructor(document: Document, options?: Options);
    destroy(): void;
    private onVisibilityChange();
}
